export enum APPLICATION_ENVIRONMENTS {
  development = 'dev',
  staging = 'staging',
  production = 'production',
}

export enum APPLICATION_STATE_VALUES {
  active = 1,
  paused = 2,
  deleted = 3,
}
